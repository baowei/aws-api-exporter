aws:
  region: us-east-1
  # 如果不设置，将使用环境变量或IAM角色
  # access_key_id: YOUR_ACCESS_KEY
  # secret_access_key: YOUR_SECRET_KEY

exporter:
  port: 9099
  address: 0.0.0.0
  metrics_path: /metrics
  collectors:
    ec2: true
    rds: true